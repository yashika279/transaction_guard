#!/usr/bin/env ruby
# frozen_string_literal: true

require "bundler/setup"
require "transaction_guard"

begin
  require "irb"
rescue LoadError
  abort "Install irb to use the console: gem install irb"
end

IRB.start(__FILE__)
